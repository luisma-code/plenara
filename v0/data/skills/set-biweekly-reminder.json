{
  "skillId": "set-biweekly-reminder",
  "displayName": "Set an every-other-week recurring reminder",
  "reads": [],
  "writes": [
    "reminder"
  ],
  "inputs": [
    { "name": "text", "required": true },
    { "name": "weekday", "required": true },
    { "name": "when", "required": true, "type": "datetime", "prompt": "What time? (e.g. \"9am\")" }
  ],
  "steps": {
    "main": [
      { "op": "compute", "fn": "now", "into": "createdAt" },
      { "op": "compute", "fn": "concat", "args": [ "biweekly:", { "var": "weekday" } ], "into": "recur" },
      {
        "op": "write_record",
        "typeId": "reminder",
        "fields": {
          "text": { "var": "text" },
          "remindAt": { "var": "when" },
          "recurrence": { "var": "recur" },
          "createdAt": { "var": "createdAt" }
        },
        "into": "newReminder"
      },
      { "op": "compute", "fn": "format_time", "args": [ { "var": "when" } ], "into": "timeLabel" },
      { "op": "format", "template": "Okay — I'll remind you to {text} every other {weekday} at {timeLabel}.", "into": "confirmationText" }
    ]
  }
}
