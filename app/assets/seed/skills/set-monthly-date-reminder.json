{
  "skillId": "set-monthly-date-reminder",
  "displayName": "Set a reminder for a day of every month",
  "reads": [],
  "writes": [
    "reminder"
  ],
  "inputs": [
    { "name": "text", "required": true },
    { "name": "day", "required": true, "type": "quantity" },
    { "name": "when", "required": true, "type": "datetime", "prompt": "What time? (e.g. \"9am\")" }
  ],
  "steps": {
    "main": [
      { "op": "compute", "fn": "now", "into": "createdAt" },
      { "op": "compute", "fn": "concat", "args": [ "monthlyday:", { "var": "day" } ], "into": "recur" },
      {
        "op": "write_record",
        "typeId": "reminder",
        "fields": {
          "text": { "var": "text" },
          "remindAt": { "var": "when" },
          "recurrence": { "var": "recur" },
          "createdAt": { "var": "createdAt" }
        },
        "into": "newReminder"
      },
      { "op": "compute", "fn": "ordinal_suffix", "args": [ { "var": "day" } ], "into": "dayLabel" },
      { "op": "compute", "fn": "format_time", "args": [ { "var": "when" } ], "into": "timeLabel" },
      { "op": "format", "template": "Okay — I'll remind you to {text} on the {dayLabel} of every month at {timeLabel}.", "into": "confirmationText" }
    ]
  }
}
