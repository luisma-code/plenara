name: plenara_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# Version tracks the release scheme (releases/VERSIONS.md): vN -> 0.N.0+N. Bump per release so
# CFBundleShortVersionString / Windows file version / notarization records aren't all "1.0.0".
version: 0.8.0+8

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  plenara:
    path: ../v0
  flutter_local_notifications: ^22.0.0
  flutter_local_notifications_windows: ^3.1.1
  timezone: ^0.11.1
  speech_to_text: ^7.4.0
  sherpa_onnx: ^1.13.4
  record: ^7.1.1
  flutter_tts: ^4.2.2 # on-device TTS (WinRT/SAPI on Windows, AVSpeechSynthesizer on Apple)
  share_plus: ^10.1.4
  path_provider: ^2.1.6
  url_launcher: ^6.3.2

# path_provider_foundation 2.6.0 (pulled in by share_plus) migrated to the experimental native-assets
# build-hook path via package:objective_c. That hook shells out to xcrun and parses its stdout, which
# a stale Xcode keychain-credential warning corrupts — breaking `flutter build --release` on iOS. Pin
# to the last classic-plugin release so NO native-assets phase runs. Revisit once native-assets is
# stable + the keychain account is cleaned. See app/ios notes.
dependency_overrides:
  path_provider_foundation: 2.4.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  # Real-device (real engine/GPU) tests — the ONLY place the animated presence + comet-trail
  # offscreen buffer (toImageSync) actually rasterizes. Headless flutter_test can't fault there,
  # which is how the list-reply raster crash slipped through. See integration_test/.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  # Windows installer packaging (RELEASING.md #5): `dart run msix:create` produces a signed,
  # identity-bearing MSIX. Identity is what gives the app a real AUMID (so reminder cancel actually
  # works, vs the no-op on the unpackaged zip) and declares the VC++ dependency. For dogfood the
  # tool signs with a generated test cert; a real Developer cert for public distribution.
  msix: ^3.16.8

# MSIX packaging config (consumed by `dart run msix:create`). Version is the pubspec version with a
# 4th part (msix requires x.y.z.0). See RELEASING.md for the signing/distribution steps.
msix_config:
  display_name: Plenara
  publisher_display_name: Plenara
  identity_name: com.plenara.app
  msix_version: 0.8.0.0
  capabilities: internetClient
  # Validated: `dart run msix:create` produces a signed 22.8 MB plenara_app.msix. For a
  # non-interactive build (CI), add `--install-certificate false` so the tool doesn't prompt to
  # trust the generated test cert. For public distribution supply a real Developer cert via
  # certificate_path/certificate_password instead of the generated test cert.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Built-in capability defs, mirrored from v0/data by tool/sync_seed.sh (v0 is pure Dart and can't
  # declare Flutter assets). Extracted on first run (seed_assets.dart) so a shipped binary seeds
  # itself with no repo present. tool/precheck.sh guards these against drift from v0/data.
  assets:
    - assets/seed/corpus.json
    - assets/seed/types/
    - assets/seed/skills/
    - assets/seed/templates/
    - assets/seed/automations/
    - assets/seed/reference/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
